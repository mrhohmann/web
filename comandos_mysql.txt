COMANDOS MYSQL
// lista todos os bancos de dados existentes no mysql

SHOW DATABASES;

// para acessar um banco
USE MYSQL;

// para listar todas as tabelas
SHOW TABLES;

// para descrever os dados de uma tabela

DESC nome_da_tabela

//para criar um banco de dados
CREATE DATABASE nome_do_banco

// exemplo:
CREATE DATABASE MundoVerde;

USE MundoVerde

// vamos criar a tabela produtos
CREATE TABLE Produtos (
  id INT PRIMARY KEY AUTO_INCREMENT,
  descricao VARCHAR(60) NOT NULL,
  categoria INT NOT NULL,
  preco FLOAT NOT NULL,
  quantidade INT NOT NULL,
  url VARCHAR(255)
);

// Categoria
// 0 = Cereais
// 1 = Suplementos
// 2 = Temperos

USE MundoVerde;
DESC Produtos;

// para inserir os dados no banco
INSERT INTO Produtos Values (1, 'Granola com castanhas 1 kg', 0, 40.18,10, 'cereais.png');

SELECT * FROM Produtos;







